# CodeAgent Context

CodeAgent owns Claude Code CLI execution.

Use allowed workspace roots only. For first-version validation, prefer `dryRun: true` unless the user explicitly asks to run Claude Code.
